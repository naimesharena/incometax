# Core: schema validation of the generated ITR-1 JSON
jsonschema>=4.17
# Web application (guided filing wizard)
flask>=2.3
# Detailed computation sheet exports
reportlab>=4.0
python-docx>=1.1
# Capital-gains Excel import/export + Form 26AS PDF / AIS ZIP unlocking
openpyxl>=3.1
pypdf>=4.0
cryptography>=42.0
# Development / tests
pytest>=7.4
# Tooling: VBA/structure extraction from the official .xlsm utilities
oletools>=0.60
